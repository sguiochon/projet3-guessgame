package sam.guessgame.model.player;

import org.junit.Test;

public class DecoderTest {

    @Test
    public void givenNothing_WhenCreatingADecoder_ThenItMustConsiderAsCandidatAllSymbols(){
/*
        Decoder decoder = new Decoder(2, new String[]{"A", "B", "C", "D"});
        Assert.assertTrue(decoder.getCandidat().toString().equals("(A,B,C,D)-(A,B,C,D)"));

        decoder = new Decoder(3, new String[]{"A", "B", "C", "D", "E", "F"});
        Assert.assertTrue(decoder.getCandidat().toString().equals("(A,B,C,D,E,F)-(A,B,C,D,E,F)-(A,B,C,D,E,F)"));

        decoder = new Decoder(1, new String[]{"A", "B", "C", "D", "E", "F"});
        Assert.assertTrue(decoder.getCandidat().toString().equals("(A,B,C,D,E,F)"));
        */
    }
}
